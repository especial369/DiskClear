# DiskClear 盘清 — 产品需求文档（PRD）

> 版本：v1.1  
> 日期：2026-09-06  
> 状态：修订稿（基于 v1.0 需求评审结论修订）  
> 前序版本：v1.0（初稿，保留存档）

---

## 修订记录（v1.0 → v1.1）

| # | 章节 | 改动 | 原因 |
|---|------|------|------|
| 1 | 6.2 | 不可删除白名单由"整目录"改为"系统关键子路径 + 例外清单"机制 | v1.0 中 2.2 缩略图缓存清理路径落在 6.2 白名单目录内，自相矛盾 |
| 2 | 6.1 | 恢复区改为按盘符分布，新增 FAT32 超 4GB 策略与自愈说明 | 固定 `%LOCALAPPDATA%` 导致跨盘清理慢、清理工具反占 C 盘、FAT32 4GB 限制无法移入 |
| 3 | 2.2 | Downloads 移出一键清理扫描项 | 风险"高"与"3 步安全清理"产品承诺冲突 |
| 4 | 6.4（新增） | 文件占用（锁定）检测与跳过策略 | 清理类工具最高频失败场景，v1.0 未覆盖 |
| 5 | 2.6 | 应用卸载降级为简单版，注册表清理移出 v1 | 残留识别无可靠通用算法，工作量与误删风险被低估（已评审确认） |
| 6 | 3.1 / 5.3 | 性能指标标注 SSD 前提；数据结构补充聚合分页策略 | HDD 无法达标；全量文件列表与内存 ≤200MB 冲突 |
| 7 | 2.7 | 定时清理、开机自启标记为 v2 | 隐含常驻后台，与轻量定位、杀软误报风险冲突 |
| 8 | 3.3 / 3.4 / 5.1 | 修正离线承诺表述，安装包内嵌 WebView2 引导组件 | Tauri 依赖 WebView2，部分 Win10 机器未预装且需联网下载 |
| 9 | 6.5（新增） | OneDrive 占位文件 / junction 循环 / 长路径 / 热拔插边界场景 | 高频真实环境问题，v1.0 遗漏 |
| 10 | 7 | 签名证书与杀软白名单申请提前至 M2 并行启动 | 采购与申诉周期以周计，放 M4 太晚 |
| 11 | 8 | 风险表补 4 条（占用 / OneDrive / junction / 杀软周期） | 同上 |
| 12 | 2.4 | 推荐保留规则定优先级；哈希增加首 4KB 预筛 | 双标准无优先级；全量哈希 IO 大，可预筛优化 |
| 13 | 2.3 | 大文件删除与恢复区上限的交互策略 | 单文件可超 5GB，策略未定义 |
| 14 | 4.1 | 设计稿标注为风格参考，附错误清单 | AI 生成稿含大量拼写与数据错误，不可作开发依据 |

---

## 1. 产品概述

### 1.1 产品定位
DiskClear（盘清）是一款面向 Windows 用户的轻量级盘符清理工具，帮助用户快速识别并安全清理磁盘中的垃圾文件、大文件、重复文件，释放存储空间。

**核心差异化卖点**：
- **零误删可恢复**：所有删除先进应用恢复区，可随时撤销
- **纯本地隐私**：扫描与清理全部本地完成，无联网、无上传、无常驻后台（v1）

### 1.2 产品目标
- 让普通用户 3 步内完成磁盘清理（扫描 → 勾选 → 清理）
- 清理过程零误删，支持撤销恢复
- 可视化呈现磁盘占用，让用户"看得见"空间去哪了

### 1.3 目标用户
| 用户类型 | 特征 | 核心诉求 |
|----------|------|----------|
| 电脑小白 | C 盘爆红但不懂删什么 | 一键清理、安全无忧 |
| 办公用户 | 磁盘文件杂乱 | 大文件查找、重复文件清理 |
| 极客/开发者 | 多盘符、缓存多 | 深度扫描、自定义规则 |

---

## 2. 功能需求

### 2.1 功能总览

```
DiskClear
├── 一键清理（核心）
├── 大文件查找
├── 重复文件检测
├── 磁盘空间分析（可视化）
├── 应用卸载（简单版）
└── 设置中心
```

### 2.2 一键清理

**功能描述**：扫描系统常见垃圾文件并安全清理。

**扫描项清单**：
| 类别 | 路径/来源 | 风险等级 | 特殊处理 |
|------|-----------|----------|----------|
| 系统临时文件 | `%TEMP%`、`C:\Windows\Temp` | 低 | 占用文件跳过（见 6.4） |
| 回收站 | 各盘 `$Recycle.Bin` | 低 | — |
| 浏览器缓存 | Chrome/Edge/Firefox 缓存目录 | 低 | 检测浏览器进程（见 6.4） |
| Windows 更新缓存 | `C:\Windows\SoftwareDistribution\Download` | 中 | 检测 wuauserv 服务（见 6.4） |
| 系统日志 | `C:\Windows\Logs` | 低 | 占用文件跳过（见 6.4） |
| 缩略图缓存 | `%LOCALAPPDATA%\Microsoft\Windows\Explorer\thumbcache_*.db` | 低 | 属 6.2 白名单例外项 |

> **v1.1 变更**：下载文件夹（Downloads）从一键清理扫描项中移除。用户下载内容误删后果重（合同、安装包、个人素材），与"3 步安全清理"的产品承诺冲突；改为仅在大文件查找与空间分析中自然呈现，由用户主动处理。同时，缩略图缓存路径细化为具体缓存文件通配项，与 6.2 白名单例外清单对齐（原 v1.0 中该项路径与白名单目录冲突）。

**交互流程**：
1. 用户选择要扫描的盘符（默认 C 盘）
2. 点击"开始扫描"，显示进度条 + 实时已发现可清理大小
3. 扫描完成，按类别展示文件数量和可释放空间；若存在被占用跳过的文件，标注"N 项因占用跳过"（见 6.4）
4. 用户勾选/取消勾选类别
5. 点击"立即清理"，弹出确认框
6. 清理完成，显示"已释放 XX GB"（净释放 = 清理大小 − 移入恢复区大小，恢复区在异盘时不计）

### 2.3 大文件查找

**功能描述**：扫描指定盘符中超过设定阈值的大文件，帮助用户定位空间占用大户。

**规则**：
- 默认阈值：100MB（可在设置中调整：50MB / 100MB / 500MB / 1GB）
- 按文件大小降序排列
- 显示：文件名、路径、大小、类型、最后修改时间
- 操作：打开所在文件夹 / 删除 / 加入忽略列表

**删除策略（v1.1 新增）**：
- 单文件 ≤ 5GB 且恢复区所在盘剩余空间充足：移入恢复区，可撤销
- 单文件 > 5GB 或恢复区所在盘空间不足：弹出红色警示对话框，明确告知"此文件将**永久删除、无法恢复**"，用户二次确认后直接删除；用户可随时取消
- OneDrive 占位文件默认不列出，如列出则标注云图标且删除前额外警示（见 6.5）

> **v1.1 变更**：原 PRD 未定义单文件超过恢复区 5GB 上限时的行为，现明确分级策略。另将"加入白名单"统一更名为"加入忽略列表"，避免与 2.7 设置中的"白名单目录"（不扫描目录）概念混淆。

### 2.4 重复文件检测

**功能描述**：基于文件内容哈希精确识别重复文件，避免误判同名不同内容文件。

**算法**：
1. 先按文件大小分组（大小不同不可能重复）
2. 同大小文件先读取**首 4KB** 计算预筛哈希，预筛不同的直接排除（大幅减少全量读盘 IO）
3. 预筛相同的再全量计算 MD5（分块读取，单块 1MB），MD5 相同判定为重复
4. 预筛与全量哈希结果按 `(大小, 路径)` 缓存于本地库，二次扫描增量校验（大小 + 修改时间未变则复用）

**推荐保留规则（v1.1 明确优先级）**：
1. **优先**：修改时间最新的文件
2. **修改时间相同**：路径最短的文件

**展示**：
- 按"重复组"展示，每组显示文件数量和总占用
- 每组内标注推荐保留项
- 支持一键删除非推荐项（统一走恢复区）

### 2.5 磁盘空间分析

**功能描述**：以矩形树图（Treemap）可视化展示目录占用比例。

**交互**：
- 顶层展示各盘符占用
- 点击盘符下钻到目录层级（按需加载，配合 5.3 聚合策略）
- 矩形面积 = 目录大小，颜色区分文件类型
- 鼠标悬停显示目录名和占用大小
- 右键菜单：打开目录 / 扫描大文件 / 删除（走恢复区）

**性能约束（v1.1 新增）**：单屏渲染节点数上限 2000，超出部分聚合为"其他"节点；下钻时懒加载子目录，禁止一次性渲染全盘树。

### 2.6 应用卸载（v1.1 降级为简单版）

**功能描述**：列出已安装程序，调起卸载并做目录级残留提示。

**v1 范围**：
1. **列出程序**：读注册表 `Uninstall` 键（HKLM/HKCU）+ UWP 应用（Appx 模型），展示程序名、发布者、安装日期、估算大小
2. **卸载**：调起程序自带卸载器（`msiexec /x` 或注册表 UninstallString），等待其退出后进入残留检测
3. **残留提示**：在 `%ProgramFiles%`、`%ProgramFiles(x86)%`、`%ProgramData%`、`%LOCALAPPDATA%` 下按程序名/发布者启发式匹配残留目录，展示路径与大小，用户勾选后移入恢复区

**明确不在 v1 范围**：
- ❌ 注册表残留扫描与清理（无可靠通用识别算法，误删风险高）→ 列入 v2 规划，届时需基于安装快照（安装前后注册表 diff）方案设计
- ❌ 强制卸载 / 卸载过程拦截

> **v1.1 变更**：原 v1.0 的"扫描残留目录和注册表项"被严重低估——业界（Revo/Geek）均为启发式方案且无 100% 可靠算法，注册表误删属数据破坏性操作。经评审确认降级为目录级残留提示，注册表清理移出 v1。

### 2.7 设置中心

| 设置项 | 说明 | 默认值 | 版本 |
|--------|------|--------|------|
| 大文件阈值 | 50MB/100MB/500MB/1GB | 100MB | v1 |
| 扫描线程数 | 1-8 | 4 | v1 |
| 恢复区保留天数 | 1-30 天 | 7 天 | v1 |
| 白名单目录 | 用户添加的不扫描目录 | 空 | v1 |
| FAT32 超大文件策略 | 跳过 / 增强确认后直接删除 | 跳过 | v1 |
| 主题 | 浅色/深色/跟随系统 | 跟随系统 | v1 |
| ~~定时清理~~ | 关闭/每天/每周/每月 | 关闭 | **v2**（需常驻后台，见 1.1） |
| ~~开机自启~~ | 开关 | 关 | **v2**（同上） |

> **v1.1 变更**：定时清理与开机自启依赖常驻后台进程/计划任务，与"轻量、无常驻"定位及杀软误报风险（自启+计划任务是灰色行为特征）冲突，移至 v2 单独决策。v1 明确**无任何常驻后台进程**。另新增 FAT32 超大文件策略设置项（对应 6.1）。

---

## 3. 非功能需求

### 3.1 性能

- 扫描 500GB 盘符（已用 200GB）≤ 60 秒，**前提：SSD**；机械硬盘受随机 IO 限制不承诺该指标（预期 3-8 分钟，界面必须显示进度且可取消）
- 应用启动时间 ≤ 3 秒
- 内存占用 ≤ 200MB（依赖 5.3 聚合策略：扫描期仅驻留目录级聚合数据与文件明细索引，明细按需分页加载，单次加载上限 1 万条）

### 3.2 安全

- 所有删除文件默认先移入恢复区（应用管理的隔离目录），不直接永久删除；恢复区按盘符分布，见 6.1
- 恢复区文件 7 天后自动清除（可配置）
- 系统关键目录白名单保护，不可删除，见 6.2
- 清理操作需二次确认；**唯一可永久删除的入口**（大文件超限、FAT32 超限）必须红色警示 + 二次确认

### 3.3 兼容性

- 支持 Windows 10 / 11（64位）
- 支持 NTFS / exFAT / FAT32 文件系统：
  - exFAT：全功能支持
  - FAT32：受单文件 4GB 上限约束，超 4GB 文件无法移入恢复区，按 2.7 设置项处理（默认跳过）
- 支持内置硬盘、外接 U 盘、移动硬盘（热拔插容错见 6.5）
- Tauri 依赖 WebView2：**安装包内嵌 WebView2 离线引导组件**，未预装系统（部分 Win10）离线也可完成安装

### 3.4 隐私

- 所有扫描和清理在本地完成，不上传任何文件数据
- 不收集用户个人信息
- **核心功能无需联网即可使用**（仅应用更新、帮助文档等辅助功能需联网）
- v1 无常驻后台进程、无开机自启、无计划任务

---

## 4. 界面设计

### 4.1 设计风格
- 现代扁平化，圆角卡片
- 主色调：科技蓝 `#2563EB` + 清洁绿 `#10B981`
- 字体：系统默认（Windows: Segoe UI）
- 图标：Lucide Icons 风格

> ⚠️ **v1.1 加注——现有设计稿状态**：当前两张 AI 生成图（应用图标 + 主界面稿）**仅作风格与布局参考，不可直接作为开发依据**。主界面稿存在以下错误，UI 开发前需按本 PRD 重绘：
> - 拼写错误：`Diple Files`（应为 Duplicate Files）、`Jemp Files`（应为 Temp Files 副标题）、`Canche`（应为 Cache）、`Fried`（应为 Freed）
> - `#10B981` 色值被当作数据显示在 Windows Update 卡片上
> - 文案与数据不自洽：标注"C Drive full"但环形图仅 74%
> - 可取之处：左侧六项导航、底部状态栏、主色搭配与 PRD 4.2 一致；应用图标（磁盘+扫帚+对勾）可作为品牌视觉基础

### 4.2 页面结构
```
┌─────────────────────────────────────────┐
│  [Logo] DiskClear    [设置] [最小化] [×] │  顶部导航栏
├──────────┬──────────────────────────────┤
│          │                              │
│  一键清理 │      主内容区（动态切换）       │
│  大文件   │                              │
│  重复文件 │                              │
│  空间分析 │                              │
│  应用卸载 │                              │
│          │                              │
├──────────┴──────────────────────────────┤
│  状态：已释放 12.3 GB | 恢复区：2.1 GB   │  底部状态栏
└─────────────────────────────────────────┘
```

### 4.3 核心页面说明

**首页（一键清理）**：
- 顶部：盘符选择卡片（显示已用/总容量环形进度条）
- 中部：扫描按钮（大按钮，醒目）
- 扫描结果：分类列表，每项带勾选框 + 可释放大小；被占用跳过的项以灰色标注原因
- 底部：清理按钮 + 预计释放空间

**空间分析页**：
- 左侧：目录面包屑导航
- 右侧：Treemap 可视化区域（占页面 70%）
- 底部：当前目录详情（文件数、总大小）

**恢复区页（v1.1 明确入口）**：
- 底部状态栏"恢复区"点击进入
- 按批次分组展示：原路径、大小、删除时间、剩余保留天数
- 操作：单文件恢复 / 按批次恢复 / 手动清空

---

## 5. 技术架构

### 5.1 技术选型
| 层级 | 技术 | 理由 |
|------|------|------|
| 前端框架 | Tauri 2.x (Rust + Web) | 体积小（~10MB）、性能好、原生权限；安装包内嵌 WebView2 离线引导 |
| UI 层 | Vue 3 + TypeScript | 组件化开发，生态成熟 |
| 核心扫描 | Rust 多线程 | 高性能文件遍历和哈希计算 |
| 数据存储 | SQLite + JSON | 轻量，记录扫描历史、配置与哈希缓存 |
| 打包 | Tauri Bundler | 生成 .msi / .exe 安装包 |

### 5.2 模块划分
```
┌─────────────────────────────────────┐
│           UI Layer (Vue 3)          │
├─────────────────────────────────────┤
│         IPC Bridge (Tauri)          │
├──────────┬──────────┬───────────────┤
│  Scanner │  Cleaner │   Analyzer    │  Rust Core
│ (扫描)   │ (清理)   │   (分析)      │
├──────────┴──────────┴───────────────┤
│         Storage (SQLite)            │
└─────────────────────────────────────┘
```

### 5.3 核心数据结构

```rust
// 扫描结果项（目录级聚合，UI 默认只读聚合层）
struct ScanItem {
    category: String,       // 类别：临时文件/回收站/缓存...
    file_count: u64,        // 文件数量
    total_size: u64,        // 总大小（字节）
    risk_level: RiskLevel,  // 低/中/高
    skipped_locked: u32,    // v1.1 新增：因占用跳过的文件数
}

// 文件明细（按需分页加载，单次上限 1 万条，内存含全量索引 ≤ 索引开销）
struct FileInfo {
    path: PathBuf,
    size: u64,
    modified: SystemTime,
    hash: Option<String>,   // 重复文件检测时填充
}

// 聚合节点（空间分析 Treemap / 全盘遍历的内存驻留形态）
struct DirAggregate {
    path: PathBuf,
    total_size: u64,
    file_count: u64,
    children: Vec<DirAggregate>, // 惰性展开，下钻时按需构建
}
```

> **v1.1 变更**：原 `ScanItem.files: Vec<FileInfo>` 全量驻留内存的设计与"内存 ≤ 200MB"冲突（百万级文件即超限）。改为：扫描期仅构建目录级聚合（`DirAggregate`）+ 文件明细索引（SQLite），明细按需分页查询。

---

## 6. 安全机制详述

### 6.1 恢复区机制（v1.1 重构）

**位置：按盘符分布**
- 被清理文件移入**其所在盘**根目录下的隐藏目录：`<盘>:\$DiskClear\Recovery\<timestamp>\`（如 `D:\$DiskClear\Recovery\20260906_153000\`）
- 同盘移动为 rename 操作，零拷贝、瞬时完成
- C 盘特殊情况：若当前用户对盘根无写入权限，回退到 `%LOCALAPPDATA%\DiskClear\Recovery\<timestamp>\` 并提示
- 恢复区目录设为隐藏 + 系统属性，`recovery_index.json`（SQLite）记录原始路径、删除时间、批次号

**上限与清理**：
- 恢复区 5GB 上限**按盘分别计算**；任一盘超限时提示用户清空该盘恢复区
- 恢复区文件按保留天数（默认 7 天）自动过期清除

**FAT32 超 4GB 文件策略（v1.1 新增）**：
- 目标恢复区位于 FAT32 且单文件 > 4GB 时，无法移入
- 按设置项（2.7）处理：默认**跳过并在结果中标注**；用户可选"增强确认后直接删除"（红色警示不可恢复 + 输入确认）

**自愈（v1.1 新增）**：
- 启动时校验各盘恢复区索引与实际目录一致性；目录被用户/杀软手动删除时，自动重建空恢复区并清理失效索引，不崩溃、不阻塞

### 6.2 不可删除目录（v1.1 重构：子路径白名单 + 例外清单）

**系统关键白名单**（以下目录及其子目录默认受保护，不纳入任何自动清理，仅可在大文件查找中手动查看）：
```
C:\Windows
C:\Program Files
C:\Program Files (x86)
C:\ProgramData
C:\Users\<用户名>\AppData\Local\Microsoft
```

**例外清单机制（v1.1 新增，解决白名单与清理项的矛盾）**：白名单目录内，符合例外清单的**明确缓存文件类型**允许清理。例外匹配规则约束：
- 仅允许**文件级通配匹配**（含完整父路径），禁止目录级放行
- 每个例外项须指明所属清理类别，并经评审后加入

| 例外项 | 匹配规则 | 类别 |
|--------|----------|------|
| 缩略图缓存 | `%LOCALAPPDATA%\Microsoft\Windows\Explorer\thumbcache_*.db` | 一键清理 |
| 图标缓存 | `%LOCALAPPDATA%\Microsoft\Windows\Explorer\iconcache_*.db` | 一键清理 |

> **v1.1 变更**：原 v1.0 将 `AppData\Local\Microsoft` 整目录列为不可删除，与 2.2 清理缩略图缓存（同处该目录下）直接矛盾。现保留白名单原有保护粒度（安全侧不做放宽），通过**文件级例外清单**放行明确的缓存文件类型——白名单规则与例外清单共同构成最终的允许清理判定。新增例外项或白名单目录时须同步评审另一侧。

### 6.3 权限处理
- 启动时检测管理员权限，非管理员模式下提示"部分系统目录可能无法访问"
- 扫描时跳过无权限目录，记录到日志，不中断扫描
- `C:\Windows\SoftwareDistribution\Download` 清理需管理员权限，非管理员模式下该项标注"需管理员权限"且默认不勾选

### 6.4 文件占用（锁定）处理策略（v1.1 新增）

**通用流程**：
1. 删除/移动失败时重试 1 次（排除瞬时锁）
2. 仍失败 → 标记为"占用跳过"，计入该类别 `skipped_locked` 计数，**不中断、不报错弹窗**
3. 清理结果页展示"N 项因占用跳过（将于下次清理时重试）"

**场景专项**：
| 场景 | 检测 | 处理 |
|------|------|------|
| 浏览器缓存 | 扫描前检测 chrome.exe / msedge.exe / firefox.exe 进程 | 运行中 → 弹提示"建议关闭浏览器后清理"，用户可选择"仅清理未锁定项"或取消 |
| Windows 更新缓存 | 检测 wuauserv 服务状态 | 服务运行中且具有管理员权限 → 提示临时停止服务（清理后恢复）；无管理员权限 → 该项禁用 |
| 系统临时文件/日志 | 删除时自然感知 | 走通用跳过流程 |

### 6.5 边界场景处理（v1.1 新增）

| 场景 | 策略 |
|------|------|
| OneDrive 占位文件 | 通过文件属性（`FILE_ATTRIBUTE_RECALL_ON_DATA_ACCESS` / `FILE_ATTRIBUTE_OFFLINE`）识别云占位文件：扫描统计计入，**默认不勾选、不清理**；大文件列表中标注云图标，删除前额外警示"此文件将从云端与本地一同删除"（若为占位） |
| Junction / 符号链接 | 目录遍历**不跟随**目录联接点（`reparse point`），防止循环遍历与重复统计；文件级符号链接按普通文件统计并在重复检测中跳过 |
| 超长路径 | 所有文件操作统一走 `\\?\` 扩展路径前缀，支持 > 260 字符路径 |
| 外接盘热拔插 | 扫描中设备移除 → 中止该盘扫描并提示"设备已断开"；清理中设备移除 → 中止并校验恢复区索引一致性（部分移动的文件按 6.1 自愈流程处理） |

---

## 7. 里程碑规划（v1.1 调整）

| 阶段 | 周期 | 交付内容 |
|------|------|----------|
| M1 原型 | 第 1-2 周 | 一键清理核心（含占用跳过）+ 恢复区基础（按盘分布）可运行 Demo |
| M2 Alpha | 第 3-5 周 | + 大文件查找（含超限删除策略）+ 重复文件检测（预筛 + 缓存）+ 恢复区完善；**并行启动：代码签名证书采购、微软 Defender 误报白名单申请** |
| M3 Beta | 第 6-7 周 | + 空间分析 Treemap + 应用卸载（简单版：列表/调起卸载/目录级残留）+ 设置中心 |
| M4 发布 | 第 8 周 | 性能优化 + 安全测试（重点：恢复区数据破坏性场景）+ 安装包（含 WebView2 离线引导）+ 发布 |

> **v1.1 变更**：① 应用卸载按降级范围排期；② 签名证书与杀软白名单申请周期以周计（Defender 申诉可达数周），从 M4 提前到 M2 并行启动；③ M1 即包含恢复区基础——数据安全机制不应后置。

---

## 8. 风险与应对（v1.1 补充）

| 风险 | 影响 | 应对 |
|------|------|------|
| 误删用户重要文件 | 高 | 恢复区机制 + 白名单 + 二次确认 |
| **文件被占用导致清理失败**（v1.1 新增） | 高 | 6.4 占用检测 + 跳过计数 + 结果标注；浏览器/更新缓存专项流程 |
| **OneDrive 占位文件被误删波及云端**（v1.1 新增） | 高 | 6.5 占位属性识别 + 默认不清理 + 双重警示 |
| 扫描大硬盘耗时过长 | 中 | 多线程 + 可取消 + 增量扫描；指标标注 SSD 前提 |
| **Junction/符号链接导致循环或重复统计**（v1.1 新增） | 中 | 6.5 不跟随 reparse point |
| **杀软误报申诉周期不可控**（v1.1 新增） | 中 | M2 提前启动签名采购与白名单申请，与开发并行 |
| 权限不足导致扫描不完整 | 中 | 提权提示 + 跳过记录 + 结果标注 |
| 重复文件哈希碰撞 | 低 | 大小 + 4KB 预筛 + MD5 三重校验，实际碰撞概率可忽略 |
| 代码签名证书 + 白名单提交 | 中 | 提前至 M2 并行（见里程碑） |

---

## 附录：术语表

| 术语 | 说明 |
|------|------|
| Treemap | 矩形树图，用面积表示数值大小的可视化方式 |
| UAC | 用户账户控制，Windows 权限提升机制 |
| MD5 | 消息摘要算法，用于文件内容唯一性校验 |
| 恢复区 | 应用管理的隔离目录，被删文件暂存于此支持撤销；v1.1 起按盘符分布 |
| WebView2 | Windows 系统网页渲染组件，Tauri UI 层运行时依赖 |
| 占位文件 | OneDrive 按需文件，本地仅占位、实际内容在云端 |
| Junction | NTFS 目录联接点（reparse point），指向另一目录的链接 |
| UninstallString | 注册表中记录程序卸载命令的键值 |
