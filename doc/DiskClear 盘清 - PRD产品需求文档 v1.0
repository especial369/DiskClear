# DiskClear 盘清 — 产品需求文档（PRD）

> 版本：v1.0  
> 日期：2026-09-06  
> 状态：初稿

---

## 1. 产品概述

### 1.1 产品定位
DiskClear（盘清）是一款面向 Windows 用户的轻量级盘符清理工具，帮助用户快速识别并安全清理磁盘中的垃圾文件、大文件、重复文件，释放存储空间。

### 1.2 产品目标
- 让普通用户 3 步内完成磁盘清理（扫描 → 勾选 → 清理）
- 清理过程零误删，支持撤销恢复
- 可视化呈现磁盘占用，让用户"看得见"空间去哪了

### 1.3 目标用户
| 用户类型 | 特征 | 核心诉求 |
|----------|------|----------|
| 电脑小白 | C 盘爆红但不懂删什么 | 一键清理、安全无忧 |
| 办公用户 | 磁盘文件杂乱 | 大文件查找、重复文件清理 |
| 极客/开发者 | 多盘符、缓存多 | 深度扫描、自定义规则 |

---

## 2. 功能需求

### 2.1 功能总览

```
DiskClear
├── 一键清理（核心）
├── 大文件查找
├── 重复文件检测
├── 磁盘空间分析（可视化）
├── 应用卸载
└── 设置中心
```

### 2.2 一键清理

**功能描述**：扫描系统常见垃圾文件并安全清理。

**扫描项清单**：
| 类别 | 路径/来源 | 风险等级 |
|------|-----------|----------|
| 系统临时文件 | `%TEMP%`、`C:\Windows\Temp` | 低 |
| 回收站 | 各盘 `$Recycle.Bin` | 低 |
| 浏览器缓存 | Chrome/Edge/Firefox 缓存目录 | 低 |
| Windows 更新缓存 | `C:\Windows\SoftwareDistribution\Download` | 中 |
| 系统日志 | `C:\Windows\Logs` | 低 |
| 缩略图缓存 | `%LOCALAPPDATA%\Microsoft\Windows\Explorer` | 低 |
| 下载文件夹 | `%USERPROFILE%\Downloads`（需用户确认） | 高 |

**交互流程**：
1. 用户选择要扫描的盘符（默认 C 盘）
2. 点击"开始扫描"，显示进度条 + 实时已发现可清理大小
3. 扫描完成，按类别展示文件数量和可释放空间
4. 用户勾选/取消勾选类别
5. 点击"立即清理"，弹出确认框
6. 清理完成，显示"已释放 XX GB"

### 2.3 大文件查找

**功能描述**：扫描指定盘符中超过设定阈值的大文件，帮助用户定位空间占用大户。

**规则**：
- 默认阈值：100MB（可在设置中调整：50MB / 100MB / 500MB / 1GB）
- 按文件大小降序排列
- 显示：文件名、路径、大小、类型、最后修改时间
- 操作：打开所在文件夹 / 删除 / 加入白名单

### 2.4 重复文件检测

**功能描述**：基于文件内容哈希精确识别重复文件，避免误判同名不同内容文件。

**算法**：
1. 先按文件大小分组（大小不同不可能重复）
2. 对同大小文件计算 MD5 哈希（分块读取，单块 1MB）
3. 哈希相同判定为重复

**展示**：
- 按"重复组"展示，每组显示文件数量和总占用
- 每组内标注推荐保留项（最新修改 / 路径最短）
- 支持一键删除非推荐项

### 2.5 磁盘空间分析

**功能描述**：以矩形树图（Treemap）可视化展示目录占用比例。

**交互**：
- 顶层展示各盘符占用
- 点击盘符下钻到目录层级
- 矩形面积 = 目录大小，颜色区分文件类型
- 鼠标悬停显示目录名和占用大小
- 右键菜单：打开目录 / 扫描大文件 / 删除

### 2.6 应用卸载

**功能描述**：列出已安装程序，支持卸载并扫描残留。

**字段**：程序名、发布者、安装日期、占用大小
**操作**：卸载 → 扫描残留目录和注册表项 → 展示残留 → 清理

### 2.7 设置中心

| 设置项 | 说明 | 默认值 |
|--------|------|--------|
| 大文件阈值 | 50MB/100MB/500MB/1GB | 100MB |
| 扫描线程数 | 1-8 | 4 |
| 恢复区保留天数 | 1-30 天 | 7 天 |
| 白名单目录 | 用户添加的不扫描目录 | 空 |
| 开机自启 | 开关 | 关 |
| 定时清理 | 关闭/每天/每周/每月 | 关闭 |
| 主题 | 浅色/深色/跟随系统 | 跟随系统 |

---

## 3. 非功能需求

### 3.1 性能
- 扫描 500GB 盘符（已用 200GB）≤ 60 秒
- 应用启动时间 ≤ 3 秒
- 内存占用 ≤ 200MB

### 3.2 安全
- 所有删除文件先移入恢复区（应用管理的隔离目录），不直接永久删除
- 恢复区文件 7 天后自动清除（可配置）
- 系统关键目录硬编码白名单，不可删除
- 清理操作需二次确认

### 3.3 兼容性
- 支持 Windows 10 / 11（64位）
- 支持 NTFS / exFAT / FAT32 文件系统
- 支持内置硬盘、外接 U 盘、移动硬盘

### 3.4 隐私
- 所有扫描和清理在本地完成，不上传任何文件数据
- 不收集用户个人信息
- 无需联网即可使用核心功能

---

## 4. 界面设计

### 4.1 设计风格
- 现代扁平化，圆角卡片
- 主色调：科技蓝 `#2563EB` + 清洁绿 `#10B981`
- 字体：系统默认（Windows: Segoe UI）
- 图标：Lucide Icons 风格

### 4.2 页面结构
```
┌─────────────────────────────────────────┐
│  [Logo] DiskClear    [设置] [最小化] [×] │  顶部导航栏
├──────────┬──────────────────────────────┤
│          │                              │
│  一键清理 │      主内容区（动态切换）       │
│  大文件   │                              │
│  重复文件 │                              │
│  空间分析 │                              │
│  应用卸载 │                              │
│          │                              │
├──────────┴──────────────────────────────┤
│  状态：已释放 12.3 GB | 恢复区：2.1 GB   │  底部状态栏
└─────────────────────────────────────────┘
```

### 4.3 核心页面说明

**首页（一键清理）**：
- 顶部：盘符选择卡片（显示已用/总容量环形进度条）
- 中部：扫描按钮（大按钮，醒目）
- 扫描结果：分类列表，每项带勾选框 + 可释放大小
- 底部：清理按钮 + 预计释放空间

**空间分析页**：
- 左侧：目录面包屑导航
- 右侧：Treemap 可视化区域（占页面 70%）
- 底部：当前目录详情（文件数、总大小）

---

## 5. 技术架构

### 5.1 技术选型
| 层级 | 技术 | 理由 |
|------|------|------|
| 前端框架 | Tauri (Rust + Web) | 体积小（~10MB）、性能好、原生权限 |
| UI 层 | Vue 3 + TypeScript | 组件化开发，生态成熟 |
| 核心扫描 | Rust 多线程 | 高性能文件遍历和哈希计算 |
| 数据存储 | SQLite + JSON | 轻量，记录扫描历史和配置 |
| 打包 | Tauri Bundler | 生成 .msi / .exe 安装包 |

### 5.2 模块划分
```
┌─────────────────────────────────────┐
│           UI Layer (Vue 3)          │
├─────────────────────────────────────┤
│         IPC Bridge (Tauri)          │
├──────────┬──────────┬───────────────┤
│  Scanner │  Cleaner │   Analyzer    │  Rust Core
│ (扫描)   │ (清理)   │   (分析)      │
├──────────┴──────────┴───────────────┤
│         Storage (SQLite)            │
└─────────────────────────────────────┘
```

### 5.3 核心数据结构
```rust
// 扫描结果项
struct ScanItem {
    category: String,      // 类别：临时文件/回收站/缓存...
    file_count: u64,       // 文件数量
    total_size: u64,       // 总大小（字节）
    files: Vec<FileInfo>,  // 文件列表
    risk_level: RiskLevel, // 低/中/高
}

// 文件信息
struct FileInfo {
    path: PathBuf,
    size: u64,
    modified: SystemTime,
    hash: Option<String>,  // 重复文件检测时填充
}
```

---

## 6. 安全机制详述

### 6.1 恢复区机制
- 删除文件时，移动到 `%LOCALAPPDATA%\DiskClear\Recovery\<timestamp>\`
- 记录原始路径到 `recovery_index.json`
- 恢复区大小超过 5GB 时提示用户清空
- 支持按文件/按批次恢复

### 6.2 不可删除目录（硬编码白名单）
```
C:\Windows
C:\Program Files
C:\Program Files (x86)
C:\ProgramData
C:\Users\<用户名>\AppData\Local\Microsoft
```
以上目录及其子目录，扫描时标记为"系统目录"，不纳入自动清理，仅可在大文件查找中手动查看。

### 6.3 权限处理
- 启动时检测管理员权限，非管理员模式下提示"部分系统目录可能无法访问"
- 扫描时跳过无权限目录，记录到日志，不中断扫描

---

## 7. 里程碑规划

| 阶段 | 周期 | 交付内容 |
|------|------|----------|
| M1 原型 | 第 1-2 周 | 一键清理核心功能可运行 Demo |
| M2 Alpha | 第 3-5 周 | + 大文件查找 + 重复文件检测 + 恢复区 |
| M3 Beta | 第 6-7 周 | + 空间分析 Treemap + 应用卸载 + 设置中心 |
| M4 发布 | 第 8 周 | 性能优化 + 安全测试 + 安装包 + 发布 |

---

## 8. 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| 误删用户重要文件 | 高 | 恢复区机制 + 白名单 + 二次确认 |
| 扫描大硬盘耗时过长 | 中 | 多线程 + 可取消 + 增量扫描 |
| 权限不足导致扫描不完整 | 中 | 提权提示 + 跳过记录 + 结果标注 |
| 重复文件哈希碰撞 | 低 | MD5 + 大小双重校验，实际碰撞概率可忽略 |
| 杀毒软件误报 | 中 | 代码签名证书 + 白名单提交 |

---

## 附录：术语表

| 术语 | 说明 |
|------|------|
| Treemap | 矩形树图，用面积表示数值大小的可视化方式 |
| UAC | 用户账户控制，Windows 权限提升机制 |
| MD5 | 消息摘要算法，用于文件内容唯一性校验 |
| 恢复区 | 应用管理的隔离目录，被删文件暂存于此支持撤销 |
